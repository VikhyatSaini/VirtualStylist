apiVersion: v1
kind: Service

metadata:
  name: virtualstylist-service

spec:
  type: NodePort

  selector:
    app: virtualstylist

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007